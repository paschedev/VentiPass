const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const n = await prisma.notification.findFirst({where: {type: 'STAFF_INVITE'}}); console.log(n.metadata, typeof n.metadata); if (n.metadata) { const upd = { ...n.metadata, status: 'ACCEPTED'}; await prisma.notification.update({where: {id: n.id}, data: {metadata: upd, isRead: true}}); console.log('Updated to:', upd); } } run();
